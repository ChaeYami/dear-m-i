# Dear Mi

> **Dear Mi (Dear Me + I)** — 정신건강의학과 진료를 위한 멘탈 케어 기록 앱
>
> 병원 예약부터 상담 기록, 처방전 OCR, 복약 일정, 감정 체크인까지 진료와 관련된 모든 기록을 한 곳에서 관리합니다.
> **React Native(Expo) 앱 + Spring Boot 3 백엔드 + AWS 배포까지 1인 풀스택으로 설계·구현**했습니다.

📷 스크린샷: (홈 화면 — 오늘 일정 카드 + 감정 체크인)

---

## 1. 문제 — 왜 만들었나

정신과·심리 진료를 받는 도메인엔 일반 진료과와 다른 페인포인트가 있다는 걸 가까이서 보게 됐습니다.

- **회차 사이의 기억이 흐려진다.** 진료는 보통 2주~한 달 간격인데, 막상 진료실에 앉으면 지난번에 무슨 얘기를 했는지, "이번엔 이걸 꼭 말해야지" 했던 게 떠오르지 않습니다. 의사는 "지난번보다 어떤가요"를 묻지만 비교할 기록이 머릿속엔 없습니다. → **진료 회차별 감정·증상·준비 메모가 쌓이는 타임라인**이 필요했습니다.
- **매일 먹는 약을 챙기기 어렵다.** 꾸준한 복용이 중요한 약일수록 일상에 묻혀 "어제 약을 먹었나?"가 헷갈립니다. → **시간대별 복약 체크와 이력**으로 "먹었는지"를 한눈에.
- **지금 먹는 약이 궁금하다.** 내가 복용 중인 약의 효과나 부작용이 궁금할 때, 약 이름으로 일일이 찾아보긴 번거롭습니다. → **처방전 한 장으로 약 정보를 자동으로 채우는 OCR**로 해결.
- **그 모든 게 가장 민감한 정보다.** 진료 내용·약물 기록은 일반 메모 앱에 적기엔 부담스럽고, 보호가 전제되어야 합니다. → **민감 정보 컬럼 암호화**.

그래서 **"진료 회차 중심 타임라인 + 처방전 한 장으로 복약 자동화 + 민감정보 암호화"** 를 한 앱에 담는 것을 목표로 했습니다.

---

## 2. 핵심 기능

기능을 나열하기보다, 각 기능이 푸는 문제 중심으로 정리했습니다.

### 처방전 → 복약 자동화 (프리미엄)
처방전을 일일이 타이핑하는 대신 **사진 한 장**으로 끝내는 것을 목표로 했습니다.
사진 촬영 → S3 업로드 → **Gemini Vision OCR로 약품 추출** → e약은요 Open API로 약 정보 보강 → **복약 일정 자동 생성 제안**. 무료 사용자는 복약 일정을 수동 등록합니다.

📷 스크린샷: (처방전 OCR 결과 화면 — 추출된 약품 목록 + 복약 일정 생성 버튼)

### 진료 회차 타임라인
병원 예약(예정/완료/취소) → 진료 전 준비 메모 작성 → 진료 후 상담 기록(감정 점수 1~10 + 내용 + 태그) 저장.
일정·기록·준비 메모가 서로 연결되어 회차별로 추적됩니다.

### 감정 체크인 & 그래프
하루 1회 감정 점수 + 메모를 기록하고, 추이를 그래프로 확인합니다. (무료: 30일 / 프리미엄: 전체 + 기간 선택)

📷 스크린샷: (감정 그래프 화면)

### 복약 관리
아침/점심/저녁/취침 시간대별 복약 체크와 이력 달력. 복약 알림은 알림에서 바로 "복용/건너뜀"을 체크할 수 있습니다(알림 카테고리 액션).

### PDF 내보내기 / 백업 (프리미엄)
선택 기간의 기록을 PDF로 만들어 S3 업로드 후 Presigned URL로 다운로드 — 다음 진료 때 의사에게 보여줄 수 있도록.

### 무료 / 프리미엄 분기
| 기능 | FREE | PREMIUM |
|---|---|---|
| 진료 기록 | 200자 / 최근 2개월 | 무제한 / 전체 |
| 하루 메모 | 100자 / 최근 30일 | 무제한 / 전체 |
| 감정 그래프 | 30일 | 전체 |
| 처방전 OCR / 약품 상세 | × | ○ |
| 복약 일정 자동 생성 | 수동만 | OCR 연동 |
| PDF 내보내기 / 백업·복원 | × | ○ |

플랜 강제는 **UI가 아니라 백엔드가 책임**집니다 (3-7 참고).

---

## 3. 기술 결정과 이유

1인 개발이라 "왜 이 구조/스택을 골랐는가"가 핵심이라고 봤습니다. 주요 판단을 문제 → 결정 → 결과로 정리했습니다.

### 3-1. 클린 아키텍처 + 외부 의존성 Port 추상화
- **문제**: OCR·결제·약품 API 등 외부 의존성이 많고, 향후 교체 가능성이 높았습니다.
- **결정**: `presentation → application → domain ← infrastructure` 4계층. Domain은 순수 Java(Spring 의존성 0), Controller는 UseCase **인터페이스만** 주입, 외부 API는 Port 인터페이스로 추상화.
- **결과**: 실제로 OCR을 **Claude 구상에서 Gemini로 바꾸고 3단 폴백을 추가**할 때 도메인 코드는 손대지 않았습니다. 외부 SDK 변화가 핵심 로직으로 번지지 않습니다.
- **트레이드오프(인정)**: 1인 프로젝트엔 다소 과한 구조. 학습 목적 + 도메인 민감성으로 선택했고, 실익은 외부연동 격리에서 나왔습니다. 의존성 방향 자동 검증(ArchUnit)은 미도입 — 수동 규율로 유지.

### 3-2. 비동기 OCR 파이프라인 (@Async, 큐 없이)
- **문제**: OCR은 외부 API 호출이라 수 초가 걸립니다. 요청 스레드를 잡으면 응답성·타임아웃 문제가 생깁니다.
- **결정**: 처방전 메타만 먼저 커밋하고 `@Async("ocrTaskExecutor")` 별도 스레드에서 OCR → e약은요 조회 → 저장. 앱은 결과를 폴링. self-invocation(같은 클래스 내 호출 시 프록시 미적용) 회피를 위해 OCR 처리를 **별도 빈**으로 분리. Gemini는 `flash → pro → flash-lite` 3단 모델 폴백으로 가용성 확보. 약 정보 조회 실패는 try-catch로 무시해 OCR 결과 자체는 보존.
- **결과**: 무거운 작업이 사용자 응답을 막지 않습니다.
- **트레이드오프(인정)**: 메시지 큐(SQS/Kafka)는 **쓰지 않았습니다.** 서버 재시작 시 진행 중 OCR이 유실될 수 있고, 그건 재시도 API로 복구합니다. 규모가 커지면 큐 기반 at-least-once가 다음 단계입니다.

### 3-3. 진료 내용 컬럼 암호화 (AES-256-GCM)
- **문제**: 진료 상담 내용은 가장 민감한 데이터입니다. DB가 유출돼도 평문이 노출되면 안 됩니다.
- **결정**: JPA `AttributeConverter`로 저장 시 암호화 / 조회 시 복호화(투명 처리). IV 12바이트를 매 저장마다 `SecureRandom`으로 생성해 암호문 앞에 prepend, GCM 128비트 태그로 무결성까지 보장. JPA가 컨버터를 Spring 빈이 아닌 `new`로 생성해 `@Value` 주입이 안 되는 문제는 `EncryptionConfig`의 `@PostConstruct` static 초기화로 우회.
- **결과**: 같은 평문도 매번 다른 암호문이 되고, 기밀성+무결성을 동시에 확보.
- **트레이드오프(인정)**: 전 사용자 **단일 키**, 키 파생은 `SHA-256(rawKey)`(PBKDF2/Argon2 아님), 로테이션 없음. KMS 봉투 암호화·per-user 키는 개선 과제로 남겨뒀습니다.

### 3-4. 암호화 컬럼의 통합 검색 (가장 어려웠던 트레이드오프)
- **문제**: 진료 내용을 암호화하니 DB `LIKE` 검색이 불가능해졌습니다. 보안과 검색이 정면 충돌.
- **결정(단기)**: 날짜 범위로 DB에서 후보를 좁힌 뒤 **인메모리에서 복호화 후 `String.contains()` 매칭**. FREE의 2개월 조회 컷오프가 자연스럽게 부하 상한 역할. 평문인 체크인/준비 메모는 QueryDSL `containsIgnoreCase`로 그대로 검색.
- **개선안(문서화)**: bi-gram을 per-user salt로 해시 → JSONB `search_tokens` + GIN 인덱스로 1차 필터 → 후보만 복호화. 도입 트리거(P95 > 800ms 또는 1인 평균 1000건 초과)까지 코드 주석에 정량화해 남김.

### 3-5. 인증 — JWT + Refresh Token Rotation
- **결정**: OAuth2(Google/Apple) → 자체 JWT. Access 1h / Refresh 7d. Refresh는 **SHA-256 해시로 DB 저장**(유출 시 원문 비노출), 1유저 1세션. 갱신 시 해시 불일치(탈취 의심)면 해당 유저 refresh를 전부 무효화. 앱은 401 동시 요청 시 `isRefreshing` 플래그 + 대기 큐로 중복 갱신 방지.
- **Apple 로그인 네이티브 전환**: 웹 `ASWebAuthenticationSession`이 iOS 26에서 SIGABRT 크래시 → App Store 2.1a 거절 → `expo-apple-authentication` 네이티브로 전환하고 identityToken을 백엔드가 Apple JWKS(서명/iss/aud/exp)로 검증하도록 변경.
- **OAuth CSRF 방어**: state(nonce)를 생성해 SecureStore에 저장하고 콜백에서 대조.

### 3-6. 보안 6원칙 — 타인 리소스 접근 차단
- **결정**: 모든 UseCase 첫 단계에서 `JWT userId == 리소스 userId`를 검증하고, 불일치 시 **403이 아니라 404**를 반환해 리소스 존재 여부 자체를 숨깁니다. `findById()` 단독 사용을 금지하고 항상 `findByIdAndUserId()`. 비동기 OCR 진입점에서도 ownership을 재검증.
- **탈퇴 처리**: `users.deleted_at` 설정과 **동시에** `refresh_tokens`를 즉시 삭제 + PII 익명화(email/name/oauth/fcm) + soft delete. 30일 후 배치가 hard delete + S3 정리.
- **트레이드오프(인정)**: 404 은닉은 "진짜 없음"과 구분이 안 되는 UX 비용이 있지만 의도된 선택(보안 > 편의). 100% 누락 없음은 자동 검증되진 않아 6원칙 규율로 보완.

### 3-7. 결제 검증 + 플랜 강제
- **IAP 서버 검증**: 클라이언트를 신뢰하지 않습니다. 영수증(JWS)을 백엔드가 App Store / Google Play에 검증한 뒤에야 PREMIUM을 활성화. Apple은 production/sandbox verifier를 둘 다 인스턴스화해 실패 시 폴백(심사 환경 대응). Apple Server-to-Server 웹훅으로 갱신/만료/환불 반영. (Android 웹 결제는 토스페이먼츠 보조.)
- **플랜 강제 AOP**: `@PlanRequired(PREMIUM)` 메서드 진입 전 AOP가 구독을 조회해 FREE면 **402** 반환. UI는 안내·배너만, 강제 책임은 전적으로 백엔드.

### 3-8. 앱 상태관리 — 서버 상태와 클라 상태 분리
- **결정**: 서버 데이터는 **React Query**(캐싱/무효화/낙관적 업데이트 표준화, staleTime 기본 5분·복약 30초). 토큰·플랜 같은 순수 클라 상태만 **Zustand**로 가볍게. 토큰은 SecureStore(키체인/키스토어), 비민감 캐시는 MMKV. 백엔드 동적 쿼리는 타입 안전성 때문에 QueryDSL.

### 3-9. 배포 / 운영
- **백엔드**: `backend/**` 변경 시 GitHub Actions → ECR → **ECS Fargate** 자동 배포. Flyway(V1~V24) 버전 관리 + `ddl-auto=validate`로 스키마 드리프트 차단.
- **앱**: 네이티브 영향 파일(package.json/app.json/eas.json/plugins 등) **git diff로 판정** — 변경 없으면 `eas update`(OTA, JS-only), 있으면 `eas build`(iOS+Android) → iOS 자동 제출. fingerprint 라이브러리 대신 git diff를 쓴 건 EAS 내부 해시와 외부 도구 해시가 안 맞아 git diff가 결정적이기 때문.
- **다중 인스턴스 스케줄러**: ECS 다중 인스턴스에서 알림 스케줄러 중복 실행을 막으려 **ShedLock**(DB 락)으로 한 인스턴스만 실행.

---

## 4. 결과

- **1인으로 앱·백엔드·인프라·CI/CD 전체를 end-to-end 구현.** DB 스키마 설계부터 OCR 비동기 파이프라인, 결제 검증, 인증, 배포 자동화까지.
- Flyway 마이그레이션 V1~V24, 백엔드 REST API + RN 앱 화면 풀 구현 (3-7 기능 전부 동작).
- **App Store 심사 대응 경험**: 2.1a/2.1b 거절(Apple 웹로그인 크래시, IAP)을 받아 네이티브 로그인 전환 + IAP verifier sandbox/production 폴백으로 대응.
- **개발 기간**: 약 2개월 (2026.04.09 ~ 2026.06.05, git 히스토리 기준).
- **출시**: App Store / Google Play 출시 완료.

> ❓채워야 함: 실사용자 수 / 다운로드 수 (있다면)

---

## 5. 트러블슈팅 & 개선

> git 히스토리에서 실제로 부딪혀 해결한 문제들. 대부분 **외부 의존성·플랫폼 차이·동시성**에서 나왔습니다.

### 외부 LLM(Gemini) OCR 안정화 — "외부 AI는 언젠가 실패한다"
초기 OCR이 가장 불안정했습니다. Gemini 모델이 수시로 deprecated / 신규 사용자 차단 / 지역 이슈로 막히고, 과부하 시 503, 가끔 깨진 JSON을 뱉었습니다. (실제로 모델을 `2.5-flash ↔ 2.0 ↔ 1.5` 사이로 며칠간 십수 번 갈아끼운 흔적이 커밋에 남아 있습니다.)
- **모델 폴백**: `flash → pro → flash-lite` 3단 폴백 — `503/500/429`(과부하·차단) 응답이 오면 같은 모델을 재시도하지 않고 **다음 모델로 즉시 전환**해 OCR이 죽지 않게.
- **malformed JSON 방지**: 프롬프트에 완전한 JSON 예시를 넣어(few-shot) 출력 형식을 고정.
- **HTTP 클라이언트 교체**: WebClient → JDK HttpClient(HTTP/2), connect 10초·request 30초 타임아웃을 모델마다 강제해 무한 대기 차단.
- 배운 점: 외부 AI API는 가용성·출력 형식 모두 통제 밖 → 폴백/재시도/출력 검증을 기본값으로 설계.

### 비동기 OCR — 트랜잭션 타이밍 & 좀비 상태
- **트랜잭션 타이밍**: `@Async` OCR이 처방전 메타의 부모 트랜잭션 커밋 전에 실행돼 데이터를 못 찾던 문제 → `TransactionSynchronization.afterCommit()` 콜백으로 옮겨 **커밋 이후에만** OCR이 돌도록 수정.
- **폴링 404 레이스**: 앱이 결과를 폴링하는데 아직 레코드 커밋 전이라 404가 뜨던 레이스 처리.
- **좀비 PROCESSING**: 서버가 OCR 도중 죽으면 처방전이 PROCESSING에 영원히 멈추는 좀비 상태 → 강제 재시도 허용 + 미저장 처방전 자동 정리. (본문 3-2의 "인메모리 유실" 트레이드오프를 실제로 메운 보완책.)

### 계정 전환 시 데이터 누수 (프라이버시 버그)
민감 앱에서 가장 위험한 부류의 버그였습니다.
- 로그아웃 시 이전 계정의 **푸시 토큰이 서버에 남아, 다른 계정으로 로그인해도 이전 계정 알림이 오던** 버그 → 서버 로그아웃에서 refresh 토큰 삭제와 함께 FCM 토큰을 `null`로 비움.
- 계정 전환 시 클라 상태가 남던 leak → 로그아웃에서 `queryClient.clear()` + 구독 스토어 reset으로 캐시·플랜 정보까지 초기화.

### 날짜 / 타임존 (KST 통일)
복약·체크인·일정처럼 "오늘"이 중요한데 UTC와 로컬이 섞여 날짜가 어긋났습니다. 서버의 날짜 연산과 스케줄러를 `ZoneId.of("Asia/Seoul")` 기준으로 고정(`LocalDate.now(SEOUL)`, `@Scheduled(zone = "Asia/Seoul")`)해 "오늘"을 **KST로 통일**.

### Apple 로그인 — 플랫폼 특수성 2종
- **웹 OAuth CORS**: Apple은 콜백을 `form_post`(POST)로 보내는데 기본 설정이 이를 막아 로그인이 실패 → `/login/**`를 CORS에서 제외하고 `AppleAwareOAuth2AuthorizationRequestResolver`로 인가 요청을 커스텀.
- **iOS 26 크래시 → 네이티브 전환**: 웹 `ASWebAuthenticationSession`이 SIGABRT로 죽어 App Store 2.1a 거절 → `expo-apple-authentication` 네이티브 로그인으로 전환 (본문 3-5).

### Android 푸시 액션 버튼
Android는 서버가 보낸 알림만으로는 "복용/건너뜀" 커스텀 액션 버튼을 못 띄웁니다 → Expo Push payload에 **`categoryId`를 실어** 보내고, 앱의 `expo-notifications`가 `categoryIdentifier` 매칭으로 액션 버튼을 단 로컬 알림을 재구성하도록 처리.

### 배포 파이프라인 시행착오
- **OTA 환경변수 누락**: `eas update`(OTA)는 `eas.json`의 env를 읽지 않아 `baseURL`이 undefined가 되던 버그 → `EXPO_PUBLIC_*`를 직접 주입해 해결.
- **빌드/OTA 분기 판정**: fingerprint 라이브러리의 해시가 EAS 내부 해시와 맞지 않아, 네이티브 영향 파일 **git diff**로 판정하도록 교체 (본문 3-9).

### 그 외 외부 API 함정
- e약은요 공공 API의 **ServiceKey 이중 인코딩** 방지(WebClient queryParam 대신 직접 `URLEncoder`로 조립), 응답 버퍼 256KB→2MB 상향, 약품당 타임아웃 5초로 제한.
- 처방전 목록 조회 **N+1 제거**: 약품을 처방전별로 따로 조회하던 걸 `findByPrescriptionIdIn(ids)` 일괄 조회 후 `groupingBy`로 매핑.

---

## 6. 회고

### 배운 점
- **외부 의존성 격리의 실익을 체감.** OCR을 교체하고 폴백을 더하는데 도메인이 흔들리지 않는 경험으로, 추상화가 왜 필요한지 코드로 확인했습니다.
- **보안과 기능의 트레이드오프를 규칙으로 만드는 일.** "암호화 vs 검색"처럼 정답이 없는 충돌을 단기 해법 + 정량화된 개선 트리거로 문서화하는 방식을 익혔습니다.

### 아쉬운 점 / 개선 방향
- **테스트 커버리지**가 부족합니다. 단위·통합 테스트(Testcontainers)를 채우는 게 우선 과제.
- **아키텍처 자동 검증(ArchUnit)** 미도입 — 의존성 규칙을 수동 규율에 의존.
- **비동기 OCR이 인메모리**라 유실 가능성 → 큐 기반 at-least-once로 전환 필요.
- **암호화 단일 키** → KMS 봉투 암호화 / 키 로테이션 / per-user 키.
- **1인 개발**이라 코드리뷰·브랜치 전략 같은 협업 프로세스를 경험하지 못한 점. 계층 경계와 컨벤션 문서화로 보완하려 했습니다.

---

## 7. 사용 기술

### Mobile (Front-end)
- React Native 0.81.5 / Expo SDK 54 / TypeScript
- TanStack Query v5 (서버 상태), Zustand v5 (전역 상태)
- MMKV (로컬 캐시), expo-secure-store (토큰 보관)
- expo-notifications (**Expo Push Service** — Firebase 제거됨)
- react-native-calendars (일정/복약 달력)
- react-native-iap (iOS IAP + Android Play Billing)
- i18next + expo-localization (한국어/영어 다국어)

### Back-end
- Spring Boot 3.5 / Java 17
- Spring Security + JWT (jjwt 0.12.x)
- JPA / Hibernate + QueryDSL 5.x
- Flyway (DB 마이그레이션, V1~V24)
- MapStruct (DTO 매핑), Bucket4j (Rate Limiting)
- ShedLock (다중 인스턴스 스케줄러 락)
- AWS SDK v2 (S3, Secrets Manager)

### Database
- PostgreSQL 15 (AWS RDS)

### Infra / 외부 연동
- AWS ECS Fargate (백엔드 배포), AWS S3 (처방전 이미지·PDF), AWS Secrets Manager (운영 시크릿)
- **Google Gemini Vision API** (처방전 OCR — `gemini-2.5-flash → pro → flash-lite` 3단 폴백, 백엔드 전용 호출)
- e약은요 Open API (약품 정보, 30일 DB 캐시)
- Expo Push Service (푸시 알림)
- App Store / Google Play IAP 검증, 토스페이먼츠 (Android 웹 결제 보조)
- EAS Build / EAS Update (OTA, git-diff 기반 분기)

---

## 8. 역할

- 기획 / 설계 / 개발 전체 (**1인 풀스택**)
- DB 스키마 설계 (Flyway 마이그레이션 V1~V24)
- Clean Architecture 기반 백엔드 REST API 설계 및 구현
- React Native 앱 전체 구현
- CI/CD 파이프라인 (GitHub Actions + EAS) 구축

---

## 9. 아키텍처

### 클린 아키텍처 레이어 구조
```
com.dearmi.backend/
├── presentation/     # Controller, DTO, Mapper (HTTP 입출력)
├── application/      # UseCase interface + impl, Port (비즈니스 오케스트레이션)
├── domain/           # Entity, Repository interface (핵심 비즈니스 규칙 — 순수 Java)
├── infrastructure/   # JPA impl, External API, Config, Batch, Scheduler
└── common/           # ApiResponse, GlobalExceptionHandler, BaseTimeEntity
```

**의존성 방향 (단방향)**
```
presentation → application → domain
infrastructure → application → domain
```

### 데이터 흐름 — 처방전 OCR (대표 흐름)
1. 앱: 이미지 선택 → `GET` Presigned URL → **앱이 S3에 직접** PUT 업로드 (서버 대역폭/메모리 절약, 권한은 15분 PUT-only로 한정)
2. 앱: `POST /prescriptions`(s3Key) → 메타 저장 후 `@Async`로 OCR 트리거
3. 서버(별도 스레드): Gemini Vision 약품 추출 → 약품별 e약은요 조회(30일 캐시) → 저장
4. 앱: OCR 결과 화면이 폴링 → 완료 시 약품 목록 표시 → 복약 일정 자동 생성

### API 응답 포맷
```json
// 성공
{ "success": true, "data": { ... } }
// 실패
{ "success": false, "errorCode": "RESOURCE_001", "message": "..." }
```
상태코드 전략: 타인 리소스 404, 플랜 위반 402, 검증 400, 인증 401, 외부 서비스 502.

---

## 10. ERD 설계

📷 스크린샷: (ERD 다이어그램 이미지)

PK는 모두 UUID, soft delete는 `deleted_at TIMESTAMP NULL`. **도메인 테이블 21개** (+ 다중 인스턴스 락용 ShedLock 테이블 별도). 주요 테이블:

- `users` — 소셜 로그인(Google/Apple) 회원, 소프트딜리트
- `hospital_schedules` — 병원 예약 일정 (SCHEDULED / COMPLETED / CANCELLED)
- `counseling_records` — 상담 기록 (감정 점수 1~10, **내용 AES-256-GCM 암호화**, 태그) — `schedule_id` SET NULL on delete
- `daily_checkins` — 하루 감정 체크인 (날짜별 1회)
- `prescriptions` — 처방전 (S3 이미지, OCR 상태) — `schedule_id` SET NULL on delete
- `prescription_medications` — 처방약 목록 (처방전 CASCADE 삭제)
- `medication_schedules` — 복약 일정 (아침/점심/저녁/취침)
- `medication_slot_groups` — 복약 시간대 그룹
- `medication_logs` — 복약 이력 (medication_schedules CASCADE 삭제)
- `prep_notes` / `prep_note_linked_notes` — 상담 준비 메모 및 연결 메모 — `schedule_id` SET NULL on delete
- `daily_notes` — 하루 메모(스레드)
- `side_effect_logs` — 부작용 기록
- `subscriptions` / `subscription_histories` — 구독 상태(FREE / PREMIUM) 및 이력
- `refresh_tokens` — JWT Refresh 토큰 (해시 저장, 탈퇴 시 즉시 삭제)
- `notification_settings` / `notifications` — 알림 설정 및 발송 내역
- `audit_logs` — 변경 불가 감사 로그
- `export_jobs` — PDF 내보내기 작업 (S3 업로드, 만료시간)
- `app_versions` — 강제 업데이트 관리 (iOS/Android 분리)

> 참고: `payments_temp`(결제 임시 상태)는 IAP 전환 과정에서 **V24 마이그레이션으로 제거**됨.

---

## 11. 화면 목록

| 화면 | 설명 |
| --- | --- |
| 스플래시 | 앱 버전 체크 (`GET /api/v1/app/version`) → 강제 업데이트 분기 |
| 로그인 | Google / Apple 소셜 로그인 (Apple은 네이티브) |
| 홈 | 오늘 일정 카드, 감정 체크인, 최근 기록 요약 |
| 상담 기록 목록 | 상담 기록 리스트, 감정 그래프 |
| 상담 기록 상세/작성 | 감정 점수, 내용, 태그, 연결 일정 |
| 처방전 목록 | 처방전 카드 리스트 (OCR 상태 표시) |
| 처방전 상세/촬영 | OCR 결과 확인, 약품 목록, 복약 일정 연결 |
| 복약 일정 | 시간대별 복약 체크, 이력 달력 |
| 병원 일정 달력 | 월별 달력, 일정 등록/수정 |
| 준비 메모 | 진료 전 메모 작성, 일정 연결 |
| 감정 그래프 | 기간별 감정 추이 차트 |
| 설정 | 계정, 구독, 알림, 백업, 탈퇴 |

📷 스크린샷: (주요 화면 3~5개 모음 — 홈 / 처방전 OCR / 감정 그래프 / 복약 일정 / 상담 기록 상세)

---

## 12. 개발 환경 셋업

### 공통 사전 준비
- **Git**, **Docker Desktop**(PostgreSQL 컨테이너), **Java 17**, **Node.js LTS**

**Windows**
```powershell
# 1. 클론
git clone <repo-url>
cd dear-m-i

# 2. DB 실행
docker run -d --name dearmidb `
  -e POSTGRES_DB=dearmidb `
  -e POSTGRES_PASSWORD=<password> `
  -p 5432:5432 postgres:15

# 3. 백엔드 (로컬 프로파일)
cd backend
./gradlew bootRun --args='--spring.profiles.active=local'

# 4. 앱
cd ../dearmi-app
npm install
npx expo start
```

**macOS**
```bash
brew install openjdk@17 node watchman

git clone <repo-url>
cd dear-m-i

docker run -d --name dearmidb \
  -e POSTGRES_DB=dearmidb \
  -e POSTGRES_PASSWORD=<password> \
  -p 5432:5432 postgres:15

cd backend && ./gradlew bootRun --args='--spring.profiles.active=local'
cd ../dearmi-app && npm install

# iOS 시뮬레이터 (Xcode 필요)
npx expo run:ios
# Android
npx expo start --android
```

### 실기기 테스트 시 API 주소
실기기에서 `localhost`는 폰 자신을 가리키므로 PC 로컬 IP가 필요합니다.
```bash
ipconfig                 # Windows
ipconfig getifaddr en0   # macOS
```
`dearmi-app/src/shared/api/` 의 baseURL을 `http://192.168.x.x:8080` 으로 변경.

> Windows는 방화벽에서 8080 포트 인바운드 허용 필요:
> `New-NetFirewallRule -DisplayName "DearMI 8080" -Direction Inbound -Protocol TCP -LocalPort 8080 -Action Allow`

---

## 13. 개발 로그

> 초기 빌드 스프린트 기록입니다. 이후 작업(Firebase → Expo Push 전환, Apple 네이티브 로그인 전환, 게스트 모드, 글래스모피즘 UI, IAP 검증 등)은 git 히스토리를 참고.

| 날짜 | 내용 |
| --- | --- |
| 2026.04.09 | 프로젝트 초기 세팅 — 모노레포 구성, Spring Boot 3.5 + Expo TypeScript 환경 셋업 |
| 2026.04.09 | 클린 아키텍처 레이어 스켈레톤, JWT 자동 갱신 Axios 인터셉터, Zustand authStore, 공통 컴포넌트, RootNavigator + 강제 업데이트 게이트 |
| 2026.04.10 | DB 스키마 설계 — V1 마이그레이션, JPA Entity + Repository |
| 2026.04.10 | OAuth2 소셜 로그인 (Google/Apple) — 백엔드 JWT 발급, 앱 딥링크 토큰 수신 |
| 2026.04.10 | 병원 일정 CRUD — 캘린더 뷰, 일정 폼 |
| 2026.04.10 | 진료 기록 CRUD — 타임라인 UI |
| 2026.04.10 | 처방전 CRUD + 비동기 OCR (**Gemini Vision**) + 감사 로그 |
| 2026.04.10 | e약은요 API 연동 — 30일 DB 캐시, OCR 완료 후 약품 정보 자동 보강 |
| 2026.04.10 | 처방전 목록 아코디언 UI + 약품 상세 화면 |
| 2026.04.10 | 푸시 알림 구현 — D-1/D-0 병원 일정 알림, 복약 리마인더 |
| 2026.04.10 | 복약 일정 관리 (백엔드 + 앱) — 마이페이지, 복약 체크 |
| 2026.04.10 | 진료 준비 메모 CRUD — D-0 알림 연동 |
| 2026.04.10 | 통합 검색 — 진료 기록/체크인/준비 메모 횡단 검색 |
| 2026.04.10 | OCR 결과 → 복약 일정 자동 연동 플로우 |
| 2026.04.10 | 구독 시스템 — 플랜 제한 AOP, 구독 UI |
| 2026.04.10 | 하루 체크인 CRUD + 감정 그래프 + 트리거 태그 |
| 2026.04.10 | i18n 다국어 — 한국어/영어, 에러 메시지 + 알림 국제화 |
| 2026.04.11 | 앱 버전 체크 — 강제 업데이트 분기 |
| 2026.04.11 | 탭 구조 개편 (5-탭), 일정/체크인 UX 개선 |
| 2026.04.11 | 약품 허가 정보 API (e약은요 상세) 연동 |
| 2026.04.12 | 타임존 버그 수정, 체크인 UX 개선 |
| 2026.04.12 | 다크모드 + SUIT 폰트 + 테마 시스템 도입 |
| 2026.04.12 | 몽글몽글 UI 오버홀 — 헤더 통일, 폼 이탈 경고 |
| 2026.04.12 | 진료 기록 상세 + 복약 일정 수정 + 이력 시간대별 그룹핑 |
| 2026.04.12 | 복약 이력 캘린더 기간 하이라이트, 복약 알림 토글, 온보딩 |
| 2026.04.12 | 커스텀 Alert / DatePicker / TimePicker 전체 적용 |
| 2026.04.12 | 블러시 핑크 악센트 컬러, 로그인/온보딩/카드 디자인 개선 |
